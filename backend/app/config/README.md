# config/

Centralized, environment-driven configuration (`settings.py`). Every
setting is read from environment variables — nothing is hardcoded, which
is what makes the app portable between local dev and Render.
