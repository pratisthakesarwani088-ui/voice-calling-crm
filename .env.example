# This root file is a reference only — the app reads its actual config
# from two separate .env files (one per service), which is how Render
# expects it for a two-service deployment:
#
#   backend/.env.example   -> copy to backend/.env
#   frontend/.env.example  -> copy to frontend/.env
#
# See those files for the full list of variables.
