# layouts/

Page shells shared across routes.

- `MainLayout.jsx`      — plain shell for public pages (landing, 404)
- `AuthLayout.jsx`      — centered card shell for guest-only pages (login, signup)
- `DashboardLayout.jsx` — Module 4: authenticated app shell (Sidebar + Navbar +
  page content) used by /dashboard and every other sidebar route
